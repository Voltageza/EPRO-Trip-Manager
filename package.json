{
  "name": "e-pro-trip-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "call-logger"
  ],
  "scripts": {
    "dev": "concurrently -n server,client,calls -c blue,green,magenta \"npm run dev -w server\" \"npm run dev -w client\" \"npm run dev -w call-logger\"",
    "build": "npm run build -w client && npm run build -w call-logger",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
